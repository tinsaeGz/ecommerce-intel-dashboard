# Shared client types

Small, client-safe primitives shared by web and mobile. Generated HTTP contract
types belong in `packages/api-client`; backend domain models, persistence types,
credentials, and server-only policy do not belong here.
