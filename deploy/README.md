# Deployment

Infrastructure boundary for Docker Compose, the same-origin edge proxy,
PostgreSQL, Redis, MinIO, worker process definitions, monitoring, backups, and
runbooks. Vercel currently hosts only `apps/web`; the complete production
topology remains governed by `SDLC.md`.
